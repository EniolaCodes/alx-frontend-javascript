// Type RowID: represents a number type alias
type RowID = number;

// Interface RowElement: defines the structure for a row element
interface RowElement {
  firstName: string;
  lastName: string;
  age?: number; // Optional age field
}
